[package]
name = "play2048_wasm_ai"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
lto = true
opt-level = 3
panic = "abort"
codegen-units = 1
strip = true
